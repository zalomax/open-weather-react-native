import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        width: '100%',
        padding: 10,
    },
    row2: {
        width: '100%',
        height: 160,
        marginTop: 40,
        marginBottom: 60,
    },
});

export default styles;